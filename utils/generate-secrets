#!/bin/bash 

# Create general config file, with auto-generated secrets
# This is idempotent (we won't overwrite existsing passwords)
FILE=/etc/openstack/openstack.conf

touch ${FILE}

for k in compute_db_password identity_db_password rabbitmq_password identity_service_token compute_service_password imagestore_service_password
do
	utils/openstack-config-get --create ${FILE} secrets ${k}
done
 
